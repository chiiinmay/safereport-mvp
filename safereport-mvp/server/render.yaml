services:
  - type: web
    name: safereport-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: ADMIN_KEY
        generateValue: true
      - key: FRONTEND_URL
        sync: false
